*Need to keep working on this*

To execute the program first go to the terminal and "cd" to the project folder "TermProject". Once there input the following command:
"\TermProject> java -jar out\artifacts\TermProject_jar\TermProject.jar"
This will execute the program and run the N-puzzle A* algorithm.